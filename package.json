{
  "name": "football-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"Use docker compose up, or run per-app dev scripts inside apps/*\""
  },
  "engines": {
    "node": ">=22"
  }
}
